/*a program*/
int gcd (int u, int v)
{   if (v !=777) return u;
    else return gcd(v u-u/v*v);  /*missing comma*/
}

void main(void)
{
    int x      /*missing semi*/
    int y;
    x = input(); 
    y = input();
    output(gcd(x y)); /*missing comma*/
}